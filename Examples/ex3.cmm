int sum(int a, int b) {
    int result;
    result = a + b;
    return result;
}

void printMessage(void) {
    char message[50];
    message = "Ola, C--!";
}

int main(void) {
    int x, y, z;
    x = 10;
    y = 20;
    z = sum(x, y);

    if (z > 15) {
        z = x+y;
    } else {
        z = x-y;
    }

    return 0;
}